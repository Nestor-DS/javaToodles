<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.ns</groupId>
        <artifactId>toodles</artifactId>
        <version>0.0.1-SNAPSHOT</version>
    </parent>

    <artifactId>launcher-module</artifactId>
    <name>launcher-module</name>
    <description>Launcher para arrancar todos los modulos</description>

    <dependencies>
<!--
        <dependency>
            <groupId>com.ns</groupId>
            <artifactId>common-module</artifactId>
        </dependency>-->

        <dependency>
            <groupId>com.ns</groupId>
            <artifactId>wsdl-module</artifactId>
            <version>0.0.1-SNAPSHOT</version>
        </dependency>

        <!--<dependency>
            <groupId>com.ns</groupId>
            <artifactId>import-module</artifactId>
        </dependency>

        <dependency>
            <groupId>com.ns</groupId>
            <artifactId>status-module</artifactId>
        </dependency>-->

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <mainClass>com.ns.launcher.LauncherApplication</mainClass>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>